<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ─── Jaringan ──────────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ─── Lokasi (untuk perhitungan jadwal sholat) ─────────────────────────── -->
    <!--
      ACCESS_FINE_LOCATION: diperlukan untuk akurasi GPS yang lebih tinggi
      sehingga waktu sholat lebih presisi. Pengguna dapat menolak dan app
      akan fallback ke lokasi default (Jakarta).
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- ─── Notifikasi adzan ──────────────────────────────────────────────────── -->
    <!-- POST_NOTIFICATIONS: wajib untuk Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- RECEIVE_BOOT_COMPLETED: agar notifikasi adzan di-restore setelah HP restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!--
      SCHEDULE_EXACT_ALARM: diperlukan agar notifikasi adzan muncul tepat waktu.
      Aplikasi sholat / pengingat ibadah termasuk kategori yang diizinkan
      Google Play untuk menggunakan izin ini.
      Pada Android 12+ (API 31+): izin ini memerlukan konfirmasi user di
      Settings > Aplikasi > Akses khusus > Alarm & pengingat.
      App menangani kasus izin tidak diberikan dengan fallback ke inexact alarm.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- WAKE_LOCK: agar alarm tetap berjalan saat layar mati -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="fashohah"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ─── Notifikasi Terjadwal ─────────────────────────────────────────── -->
        <!--
          ScheduledNotificationReceiver: exported=true wajib agar AlarmManager
          bisa mengirim broadcast ke receiver saat waktu alarm tiba.
        -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="true"/>

        <!--
          ScheduledNotificationBootReceiver: exported=true wajib agar sistem
          dapat mengirim BOOT_COMPLETED. Plugin akan me-restore semua notifikasi
          terjadwal dari SharedPreferences setelah HP menyala kembali.
        -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!--
          ExactAlarmPermissionReceiver: wajib oleh Google Play policy untuk app
          yang menggunakan SCHEDULE_EXACT_ALARM (Android 12+ / API 31+).
          Receiver ini menangani event ketika user mencabut izin exact alarm
          di System Settings, sehingga app dapat merespons dengan benar.
        -->
        <receiver
            android:name=".ExactAlarmPermissionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED"/>
            </intent-filter>
        </receiver>

        <!-- Flutter embedding metadata — jangan dihapus -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
